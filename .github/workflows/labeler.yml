name: Labeler

on:
  - pull_request_target

jobs:
  label:
    permissions:
      contents: read
      pull-requests: write

    runs-on: ubuntu-latest
    steps:
      - uses: actions/lebeler@4
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
